# intelliquery-platform-94839-94848

This workspace hosts the DSP Web Frontend in `dsp_web_frontend/`.

Quick start:
1) cd dsp_web_frontend
2) npm install
3) cp .env.example .env  # adjust endpoints if needed
4) npm start

App routes:
- /login, /signup: public
- /: protected home with prompt interface
